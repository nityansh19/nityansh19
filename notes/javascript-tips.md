# JavaScript Tips

- Prefer `const` by default and `let` when reassignment is required.
- Use strict equality (`===`) unless coercion is intentional.
- Keep async code readable with `async`/`await`.
- Validate data at API and UI boundaries.
- Break large functions into small testable units.
